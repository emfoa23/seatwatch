name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lint-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Skeleton check
        run: |
          echo "seatwatch CI placeholder — web/ worker/ 구현 후 lint/typecheck/test 추가 예정"
          ls -la
